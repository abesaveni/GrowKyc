{
  "name": "growkyc",
  "private": true,
  "description": "Run all npm scripts from the Growkyc-main app folder.",
  "scripts": {
    "dev": "npm run dev --prefix Growkyc-main",
    "build": "npm run build --prefix Growkyc-main",
    "install:app": "npm install --prefix Growkyc-main"
  }
}
